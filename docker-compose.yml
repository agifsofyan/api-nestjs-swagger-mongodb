version: "3.8"
services:
    nodeserver:
        build:
            context: ./docker/node
        ports:
            - "5000:5000"
    nginx:
        restart: always
        build:
            context: ./docker/nginx
        ports:
            - "80:80"
