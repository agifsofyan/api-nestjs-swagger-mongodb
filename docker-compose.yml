version: "3.8"

services:
        api.laruno.id:
                build: .
                image: "laruno/laruno-api:latest"
                container_name: laruno
                volumes:
                        - /var/www/api.laruno.id
                ports:
                        - "4000:4000"
                networks:
                        - default

                          #nginx:
                          #restart: always
                          #build: ./docker/nginx
                          #ports:
                          #- "80:80"
                          #networks:
                          #- default
